// Application configuration
const MAX_RETRIES = 5;

/**
 * Greet someone by name.
 */
function greet(name) {
    return `Hello, ${name}!`;
}

class Calculator {
    /**
     * Add two numbers.
     */
    add(a, b) {
        return a + b;
    }

    subtract(a, b) {
        return a - b;
    }
}
