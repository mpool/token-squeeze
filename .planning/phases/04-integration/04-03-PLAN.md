---
phase: 04-integration
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, TEST-01]
must_haves:
  truths:
    - "CLAUDE.md CLI commands table matches actual commands (index, outline, extract, find — no list/purge)"
    - "CLAUDE.md has no references to project names or ~/.token-squeeze/"
    - "README.md describes local .cache/ storage model, not global ~/.token-squeeze/"
    - "README.md MCP tools table lists 3 tools (no list_projects), skills lists 2 skills (no purge)"
    - "All tests pass against final codebase state"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate project documentation"
      contains: ".cache/"
    - path: "README.md"
      provides: "Accurate user-facing documentation"
      contains: ".cache/"
  key_links:
    - from: "README.md MCP tools table"
      to: "plugin/mcp-server.js TOOLS array"
      via: "tool names and descriptions must match"
      pattern: "read_file_outline|read_symbol_source|search_symbols"
---

<objective>
Update CLAUDE.md and README.md to reflect the completed local-storage model, then verify all tests pass.

Purpose: Documentation must accurately describe the current system — no stale references to project names, global storage, list/purge commands, or auto-indexing.
Output: Accurate CLAUDE.md and README.md reflecting the final state of the milestone.
</objective>

<execution_context>
@C:/Users/MaxPool/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MaxPool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration/04-01-SUMMARY.md
@.planning/phases/04-integration/04-02-SUMMARY.md

<interfaces>
<!-- Final MCP tool names (from Plan 01) -->
Tools: read_file_outline, read_symbol_source, search_symbols
Skills: /token-squeeze:index, /token-squeeze:explore, /token-squeeze:savings
Storage: <cwd>/.cache/ (local per-project)

<!-- Current CLAUDE.md CLI table (already updated in Phase 3) -->
| Command | Description |
|---------|-------------|
| `index <path>` | Index a local folder |
| `outline <file>` | Show symbols in a file (resolves .cache from cwd) |
| `extract [id]` | Get full source of a symbol (resolves .cache from cwd) |
| `find <query>` | Search symbols by query (resolves .cache from cwd) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md and README.md for local-storage model</name>
  <files>CLAUDE.md, README.md</files>
  <action>
**CLAUDE.md:**
1. Verify CLI commands table is already correct (Phase 3 updated it). If it already shows the 4 commands without list/purge, no table changes needed.
2. In "Key Conventions" section, change `**Storage:** \`~/.token-squeeze/\` (JSON index + raw files)` to `**Storage:** \`<cwd>/.cache/\` (JSON index + raw files, per-project local cache)`.
3. In the Architecture tree, remove the hooks and scripts lines:
   - Delete `│   ├── hooks/                     # Hooks (auto-index on session start)`
   - Delete `│   ├── scripts/                   # Hook helper scripts`
4. Scan for any remaining references to `~/.token-squeeze/`, project names, list command, or purge command. Remove or update any found.

**README.md:**
Rewrite substantially. The new README should contain:

1. **Header and intro** — keep the install section as-is.

2. **What it does** section — rewrite:
   - MCP Tools table: list the 3 actual tools with correct names from mcp-server.js:
     | Tool | Description |
     |------|-------------|
     | `read_file_outline` | Show symbols in a file (functions, classes, methods, types) |
     | `read_symbol_source` | Get the full source of one or more symbols by ID |
     | `search_symbols` | Search symbols by name, signature, or docstring |
   - Skills table: list only index, explore, and savings:
     | Skill | Description |
     |-------|-------------|
     | `/token-squeeze:index` | Index a directory's symbols (run this first) |
     | `/token-squeeze:explore` | Research how code works by tracing implementations |
     | `/token-squeeze:savings` | Estimate tokens saved this session |
   - Remove the sentence about auto-indexing on session start.

3. **How it works** section — update storage description:
   Change "Symbols are stored in `~/.token-squeeze/` as JSON indexes alongside raw source snapshots." to "Symbols are stored in `<project>/.cache/` as JSON indexes alongside raw source snapshots. Each project's index lives alongside its source code."
   Remove any mention of project names.

4. Keep Supported Languages and Install sections unchanged.
  </action>
  <verify>
    <automated>grep "~/.token-squeeze" CLAUDE.md README.md && echo "FAIL: old storage path found" || echo "PASS: no old paths"; grep "list_projects\|token_squeeze_list\|purge" README.md && echo "FAIL: dead tool/skill refs" || echo "FAIL check"; grep "auto-index" README.md && echo "FAIL: auto-index ref" || echo "PASS: no auto-index"; grep ".cache/" CLAUDE.md README.md && echo "PASS: .cache/ referenced" || echo "FAIL"</automated>
  </verify>
  <done>
    - CLAUDE.md Storage line says `<cwd>/.cache/`
    - CLAUDE.md architecture tree has no hooks/ or scripts/ lines
    - README.md MCP tools table lists read_file_outline, read_symbol_source, search_symbols (no list_projects)
    - README.md skills table lists index, explore, savings (no purge)
    - README.md storage description says .cache/ not ~/.token-squeeze/
    - No auto-index mention in README
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all tests pass against final codebase</name>
  <files></files>
  <action>
Run the full test suite to confirm all 265 tests pass with the final codebase state.
Also scan test files for any remaining references to the old `~/.token-squeeze/` path pattern or project name arguments in test CLI calls.

```bash
dotnet test src/TokenSqueeze.Tests/TokenSqueeze.Tests.csproj --nologo
```

If any tests fail, investigate and fix. If any old storage path references are found in tests, update them (though Phase 2 already migrated all tests to constructor-injected temp directories, so this is a verification-only step).

Also run a quick grep to confirm no test references old paths:
```bash
grep -r "token-squeeze" src/TokenSqueeze.Tests/ --include="*.cs" | grep -v "bin/" | grep -v "obj/"
```
This should only find the namespace references (TokenSqueeze.*), not path references like `~/.token-squeeze/`.
  </action>
  <verify>
    <automated>dotnet test src/TokenSqueeze.Tests/TokenSqueeze.Tests.csproj --nologo -v q</automated>
  </verify>
  <done>All tests pass. No test files reference ~/.token-squeeze/ storage paths.</done>
</task>

</tasks>

<verification>
- `grep -r "~/.token-squeeze" CLAUDE.md README.md src/` returns no matches
- `grep "read_file_outline" README.md` confirms correct tool name
- `grep "auto-index\|list_projects\|purge" README.md` returns no matches
- `dotnet test src/TokenSqueeze.Tests/TokenSqueeze.Tests.csproj --nologo -v q` passes
</verification>

<success_criteria>
CLAUDE.md and README.md accurately describe the local-storage model with correct tool/skill names, no stale references to the old storage model, and all 265 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration/04-03-SUMMARY.md`
</output>
