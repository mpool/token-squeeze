---
phase: 04-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/hooks/hooks.json
  - plugin/scripts/auto-index.sh
  - plugin/skills/purge/SKILL.md
  - plugin/skills/index/SKILL.md
  - plugin/skills/explore/SKILL.md
  - plugin/skills/savings/SKILL.md
  - plugin/agents/codebase-explorer.md
  - plugin/agents/symbol-locator.md
autonomous: true
requirements: [PLUG-01, PLUG-02, PLUG-03, PLUG-04, PLUG-05, PLUG-06]
must_haves:
  truths:
    - "No hook files exist in plugin/hooks/ or plugin/scripts/"
    - "Purge skill directory does not exist"
    - "Index skill emphasizes run-this-first and offers .gitignore addition"
    - "Explore skill does not reference list_projects"
    - "Agent files do not reference list_projects"
  artifacts:
    - path: "plugin/skills/index/SKILL.md"
      provides: "Updated index skill without project name output"
      contains: ".gitignore"
    - path: "plugin/skills/explore/SKILL.md"
      provides: "Updated explore skill without list_projects"
  key_links:
    - from: "plugin/agents/codebase-explorer.md"
      to: "MCP tools"
      via: "tools frontmatter"
      pattern: "search_symbols.*read_file_outline.*read_symbol_source"
---

<objective>
Clean up plugin hooks, scripts, skills, and agents to match the local-storage model — delete dead files, update remaining skills and agents to remove project name references.

Purpose: Plugin surface area must reflect the simplified CLI with no project identity, no auto-index hook, no purge command.
Output: Clean plugin directory with only index/explore/savings skills, updated agents, no hooks or scripts.
</objective>

<execution_context>
@C:/Users/MaxPool/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MaxPool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration/04-RESEARCH.md

<interfaces>
<!-- Current plugin file contents the executor needs -->

From plugin/hooks/hooks.json:
```json
{ "hooks": { "SessionStart": [{ "matcher": "startup", "hooks": [{ "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/scripts/auto-index.sh" }] }] } }
```
(Delete this file and directory)

From plugin/agents/codebase-explorer.md (frontmatter tools line):
```yaml
tools: mcp__plugin_token-squeeze_token-squeeze__search_symbols, mcp__plugin_token-squeeze_token-squeeze__read_file_outline, mcp__plugin_token-squeeze_token-squeeze__read_symbol_source, mcp__plugin_token-squeeze_token-squeeze__list_projects, Grep, Glob, Read
```
(Remove the list_projects tool reference)

From plugin/agents/symbol-locator.md (frontmatter tools line):
```yaml
tools: mcp__plugin_token-squeeze_token-squeeze__search_symbols, mcp__plugin_token-squeeze_token-squeeze__read_file_outline, mcp__plugin_token-squeeze_token-squeeze__list_projects, Glob
```
(Remove the list_projects tool reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hooks, scripts, and purge skill</name>
  <files>plugin/hooks/hooks.json, plugin/scripts/auto-index.sh, plugin/skills/purge/SKILL.md</files>
  <action>
1. Delete `plugin/hooks/hooks.json`
2. Delete the `plugin/hooks/` directory (will be empty)
3. Delete `plugin/scripts/auto-index.sh`
4. Delete the `plugin/scripts/` directory (will be empty)
5. Delete `plugin/skills/purge/SKILL.md`
6. Delete the `plugin/skills/purge/` directory (will be empty)

Use `rm` for files and `rmdir` or `rm -r` for empty directories. Stage all deletions with git.
  </action>
  <verify>
    <automated>test ! -e plugin/hooks && echo "PASS: hooks/ gone" || echo "FAIL"; test ! -e plugin/scripts && echo "PASS: scripts/ gone" || echo "FAIL"; test ! -e plugin/skills/purge && echo "PASS: purge/ gone" || echo "FAIL"</automated>
  </verify>
  <done>
    - plugin/hooks/ directory does not exist
    - plugin/scripts/ directory does not exist
    - plugin/skills/purge/ directory does not exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Update skills and agents to remove project name references</name>
  <files>plugin/skills/index/SKILL.md, plugin/skills/explore/SKILL.md, plugin/skills/savings/SKILL.md, plugin/agents/codebase-explorer.md, plugin/agents/symbol-locator.md</files>
  <action>
**plugin/skills/index/SKILL.md:**
Replace the Output section with:
```markdown
## Output

The command outputs JSON. Present a summary to the user showing:
- Number of files indexed
- Languages detected
- Cache location

Then offer: "Want me to add `.cache/` to your `.gitignore`?"
```
This removes "Project name assigned" and adds the .gitignore offer. No other changes needed — the Action section is already correct (just runs `<binary> index <path>`).

**plugin/skills/explore/SKILL.md:**
Replace Step 1 with:
```markdown
## Step 1 — Validate Index

Use `search_symbols` with a broad query (e.g. the user's topic) to confirm an index exists. If the MCP server returns "No index found", tell the user to run `/token-squeeze:index` first and stop.
```
This replaces the `list_projects` call with a search_symbols probe.

**plugin/skills/savings/SKILL.md:**
Review only — this file has no project name references. Leave unchanged.

**plugin/agents/codebase-explorer.md:**
1. In the `tools:` frontmatter line, remove `mcp__plugin_token-squeeze_token-squeeze__list_projects, ` (keep the trailing space cleanup clean — the remaining tools should be comma-separated without double-comma or trailing comma).
2. In the "Your Tools — Use in This Order" section, delete the numbered item:
   `1. **list_projects** — Confirm the index exists and get the project name`
   Renumber remaining items to start at 1.
3. In Step 1, replace the current text that says "Use `search_symbols` to find the symbols" — actually Step 1 doesn't reference list_projects, so just remove it from the ordered tools list.

**plugin/agents/symbol-locator.md:**
1. In the `tools:` frontmatter line, remove `mcp__plugin_token-squeeze_token-squeeze__list_projects, `.
2. In "Your Tools" section, delete:
   `1. **list_projects** — Get the project name from the index`
   Renumber remaining items to start at 1.
  </action>
  <verify>
    <automated>grep -r "list_projects" plugin/skills/ plugin/agents/ 2>/dev/null | grep -v ".cache" && echo "FAIL: list_projects references remain" || echo "PASS: no list_projects refs"; grep "Project name assigned" plugin/skills/index/SKILL.md && echo "FAIL" || echo "PASS: project name removed from index skill"; grep "gitignore" plugin/skills/index/SKILL.md && echo "PASS: gitignore offer present" || echo "FAIL"</automated>
  </verify>
  <done>
    - Index skill has no "Project name assigned" text and includes .gitignore offer
    - Explore skill uses search_symbols for index validation instead of list_projects
    - Savings skill unchanged (already clean)
    - Both agent files have no list_projects in tools frontmatter or body text
  </done>
</task>

</tasks>

<verification>
- `find plugin/hooks plugin/scripts plugin/skills/purge -type f 2>/dev/null` returns nothing
- `grep -r "list_projects" plugin/` returns no matches
- `grep -r "Project name" plugin/skills/` returns no matches
- `grep "gitignore" plugin/skills/index/SKILL.md` finds the offer
- `dotnet test src/TokenSqueeze.Tests/TokenSqueeze.Tests.csproj --nologo -v q` still passes (C# unaffected)
</verification>

<success_criteria>
Plugin directory has no hooks, scripts, or purge skill. Remaining skills and agents reference only the 3 surviving MCP tools with no project name concept.
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration/04-02-SUMMARY.md`
</output>
